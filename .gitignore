# Gradle / Android — não commitar
local.properties
.gradle/
build/
app/build/
*.iml
.DS_Store

# Assinatura — nunca commitar keystore/senhas
*.jks
*.keystore
keystore.properties

# APKs locais (baixados do Actions, não versionar)
apk/
*.apk
